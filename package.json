{
  "name": "blackrockcity-kids",
  "version": "0.1.0",
  "description": "Praktische informatie voor ouders die met kinderen naar Black Rock City gaan",
  "license": "CC-BY-4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy && node scripts/bundel.mjs _site",
    "clean": "rm -rf _site",
    "bundel": "node scripts/bundel.mjs",
    "test": "node scripts/controleer.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "js-yaml": "^5.4.2"
  }
}
